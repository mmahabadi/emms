import {FC} from 'react';
import {UseFormReturn} from "react-hook-form/dist/types";
import {TextInput} from "@emms/ui-kit";

type Props = {
  form: UseFormReturn
}
export const JobRequestFilter: FC<Props> = ({form}) => {
  return (
    <>
    </>
  )
}
