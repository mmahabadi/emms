import {FC} from 'react';
import {UseFormReturn} from "react-hook-form/dist/types";
import {TextInput} from "@emms/ui-kit";

type Props = {
  form: UseFormReturn
}
export const JobOrderFilter: FC<Props> = ({form}) => {
  return (
    <>
    </>
  )
}
